node_modules/
dist/
coverage/
test-results/
*.tsbuildinfo

# Briefs scorecard run outputs — git-ignored operator debug artifacts
# (loops/briefs/decisions/transcript dumps), never committed or formatted
evals/briefs/runs/

# iOS build artefacts + generated Xcode project
ios/.build/
ios/build/
ios/Omnesis.xcodeproj/
ios/DerivedData/

# Generated SwiftLint baseline — managed by `swiftlint`, not prettier
.swiftlint-baseline.json

# Cross-client wire corpus is byte-for-byte generated by `npm run wire-fixtures`.
# Its generator and parity tests own the JSON serialization.
wire-fixtures/*.json
ios/Tests/OmnesisTests/wire-fixtures/*.json

# Portal is standalone vanilla JS/CSS served as static files; keep it as-is
packages/gateway/portal/

# Vendored binary blobs / large fixtures
**/*.gguf
**/*.db
**/*.db-journal
**/*.db-wal
**/*.db-shm

# Shell scripts — no prettier parser
**/*.sh

# Lockfiles — formatter would churn them and offer no value
package-lock.json
**/package-lock.json
pnpm-lock.yaml

# Native Android client — Kotlin/Gradle tree, not part of the TS/web tooling.
android/
ios/build-device/
ios/build/

# The CLA action rewrites this file in its own shape; formatting it would
# make the next signature fail format-check for whoever pushes after it.
signatures/
