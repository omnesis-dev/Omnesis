---
"omnesis": patch
---

The Android app now cancels a session's launch reads when the session ends. After re-pairing or unpairing, a read still in flight no longer reaches the old gateway or refills the source list the app had just cleared.
