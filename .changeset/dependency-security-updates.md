---
"omnesis": patch
---

Development dependencies are updated for published security advisories: the build tooling's TOML parser, and the OpenClaw release the OpenClaw integration is checked against, now 2026.7.35, which carries patched URI, IP-address and HTTP-client libraries. The gateway, collector, CLI and apps ship no changed runtime dependency, and the OpenClaw integration still supports OpenClaw 2026.7.1 and later.
